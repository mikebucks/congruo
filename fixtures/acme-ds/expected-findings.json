{
  "description": "Gold-corpus truth: exact expected findings when the CodeAdapter extract is analyzed with an empty Figma side and no statuses. The P/R gate asserts these types match EXACTLY — a miss is a recall bug, an extra is a precision bug. MISSING_IN_FIGMA is excluded (trivially every component, verified by unit tests).",
  "expected": {
    "HARDCODED_VALUE_CODE": [
      {
        "component": "Button",
        "note": "#ff5733 borderColor, aggregated to one finding"
      }
    ],
    "REDUNDANT_COMPONENT": [
      {
        "component": "ButtonNew",
        "note": "near-duplicate of Button, lands on the lesser-used side"
      }
    ],
    "UNUSED_PROP": [
      { "component": "Tag", "note": "rounded never set" },
      { "component": "Banner", "note": "dismissible never set" },
      {
        "component": "Badge",
        "note": "tone never set (stories are not usages)"
      }
    ],
    "UNUSED_COMPONENT": [
      { "component": "Stepper", "note": "zero usages" },
      { "component": "ButtonNew", "note": "zero usages" },
      {
        "component": "CheckIcon",
        "note": "svg asset, never imported (StarIcon is the used control)"
      }
    ],
    "SINGLE_FILE_ADOPTION": [
      { "component": "Card", "note": "2 usages, both in Home.tsx" }
    ],
    "NO_STORY": [
      { "component": "Banner" },
      { "component": "Button" },
      { "component": "ButtonNew" },
      { "component": "Card" },
      { "component": "Input" },
      { "component": "Stepper" },
      { "component": "Tag" }
    ],
    "PROPS_UNDOCUMENTED": [
      { "component": "Banner", "note": "no JSDoc at all" },
      { "component": "Button", "note": "label lacks JSDoc" },
      { "component": "ButtonNew" },
      { "component": "Card" },
      { "component": "Stepper" }
    ],
    "NO_USAGE_GUIDANCE": [
      { "component": "Banner" },
      { "component": "Button" },
      { "component": "ButtonNew" },
      { "component": "Card" },
      { "component": "Input" },
      { "component": "Tag" }
    ]
  },
  "negativeControls": [
    {
      "component": "Badge",
      "note": "documented + storied + used: only UNUSED_PROP(tone)"
    },
    { "component": "Button", "note": "used in 2 files: no adoption findings" },
    {
      "component": "Input",
      "note": "fully documented props: no PROPS_UNDOCUMENTED"
    }
  ],
  "propMappingCases": [
    { "figma": "Disabled", "code": "isDisabled", "component": "Input" },
    { "figma": "Size=lg", "code": "size=\"lg\"", "component": "Input" }
  ]
}
