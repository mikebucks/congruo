export const theme = {
  colors: {
    border: "var(--color-border)",
    primary: "var(--color-primary)",
  },
  spacing: {
    sm: "var(--space-100)",
    md: "var(--space-200)",
    lg: "var(--space-400)",
  },
} as const;
