services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: congruo
      POSTGRES_PASSWORD: congruo
      POSTGRES_DB: congruo
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
