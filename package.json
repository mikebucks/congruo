{
  "name": "congruo",
  "private": true,
  "packageManager": "pnpm@10.30.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "check": "turbo run build typecheck test && biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.10",
    "turbo": "^2.10.11",
    "typescript": "catalog:"
  }
}
